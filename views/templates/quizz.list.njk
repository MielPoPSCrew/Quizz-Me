{% if quiz|length != 0 %}
<table class="highlight responsive-table">

    <thead>
    <tr>
        <th>Nom</th>
        <th>Thème</th>
        <th></th>
    </tr>
    </thead>

    <tbody>

    {% for q in quiz %}
        <tr>
            <td>{{ q.name }}</td>
            <td>{{ q.topic.name }}</td>
            <td><a href="/games/create?quiz={{ q._id }}" class="waves-effect waves-teal btn">Jouer</a></td>
        </tr>
    {% endfor %}

    </tbody>

</table>
{% else %}
    <h5 class="center-align">Aucun quiz sur le site :(</h5>
{% endif %}
